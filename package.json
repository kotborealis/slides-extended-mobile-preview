{
  "name": "slides-extended-mobile-preview",
  "version": "0.6.0",
  "description": "Standalone Reveal.js preview for Markdown slide decks in Obsidian Mobile.",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "lint": "eslint .",
    "test": "rm -rf .test-build && tsc -p tsconfig.test.json && node --test .test-build/**/*.test.js",
    "typecheck": "tsc --noEmit",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "revealjs",
    "slides",
    "mobile"
  ],
  "author": "kotborealis <kotborealis@awooo.ru>",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^24.10.1",
    "builtin-modules": "^5.0.0",
    "esbuild": "^0.27.0",
    "eslint": "^9.39.1",
    "obsidian": "^1.8.7",
    "reveal.js": "^5.2.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.48.0"
  }
}
